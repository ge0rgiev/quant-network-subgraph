{
  "name": "quant-network",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ quant-network",
    "create-local": "graph create --node http://localhost:8020/ quant-network",
    "remove-local": "graph remove --node http://localhost:8020/ quant-network",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 quant-network"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
